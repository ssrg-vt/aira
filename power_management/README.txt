1. Getting started

Want to get started with PowerManager?  Build the executable:

$ cd <PowerManager root>/src
$ make

For low-level control of PowerManager, run with -h to see available options:

$ ./PowerManager -h

For daemon management, use the provided scripts:

$ <PowerManager root>/scripts/manage_daemon <start | stop | -h/--help>

